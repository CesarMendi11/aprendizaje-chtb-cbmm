# demo: snapshot local, sin HTTP. live: API FastAPI mediante /api.
VITE_ADMIN_API_MODE=demo

# Target usado por el proxy de Vite para reenviar /api al backend FastAPI.
VITE_ADMIN_API_TARGET=http://127.0.0.1:8000